---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "algolia_ingestion_destination Data Source - algolia"
subcategory: ""
description: |-
  Use this data source to read an Algolia Ingestion destination's configuration, including its input: like the algolia_ingestion_source data source, the Ingestion API does not redact a destination's configuration.
---

# algolia_ingestion_destination (Data Source)

Use this data source to read an Algolia Ingestion destination's configuration, including its `input`: like the algolia_ingestion_source data source, the Ingestion API does not redact a destination's configuration.

## Example Usage

```terraform
terraform {
  required_providers {
    algolia = {
      source = "registry.terraform.io/algolia/algolia"
    }
  }
}

provider "algolia" {
  analytics_region = "us"
}

# Reads an existing destination's configuration, including `input` and
# `transformation_ids`: like algolia_ingestion_source, the Ingestion API
# does not redact a destination's configuration.
data "algolia_ingestion_destination" "example" {
  destination_id = "6c02aed6-9d1f-4fef-b2f6-9a5a3ea1e2f2"
}

output "destination_type" {
  value = data.algolia_ingestion_destination.example.type
}

output "destination_input" {
  value = data.algolia_ingestion_destination.example.input
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `destination_id` (String) Universally unique identifier (UUID) of the destination to read.

### Read-Only

- `authentication_id` (String) Universally unique identifier (UUID) of the authentication resource this destination uses to connect to its underlying platform, if any.
- `created_at` (String) Date and time when the resource was created, in RFC 3339 format.
- `id` (String) Terraform identifier for the resource. Equal to `destination_id`.
- `input` (String) JSON-encoded configuration matching `type`.
- `name` (String) Descriptive name for the destination.
- `transformation_ids` (List of String) Universally unique identifiers (UUIDs) of the transformations applied to records before they reach this destination, in order.
- `type` (String) Type of destination.
- `updated_at` (String) Date and time when the resource was last updated, in RFC 3339 format.
