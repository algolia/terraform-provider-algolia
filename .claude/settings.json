{
  "enabledPlugins": {
    "code-simplifier@claude-plugins-official": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo $CLAUDE_FILE_PATHS | tr ' ' '\\n' | grep '\\.go$' | xargs -r gofmt -w 2>/dev/null || true"
          },
          {
            "type": "command",
            "command": "echo $CLAUDE_FILE_PATHS | tr ' ' '\\n' | grep -q '\\.go$' && go vet ./... 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
